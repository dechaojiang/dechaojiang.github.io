<div class="news">
    {% assign categorized_visuals = site.visuals | where: "category", "Visuals" %}
    {% assign sorted_visuals = categorized_visuals | sort: "importance" %}

    <div class="container">
      <div class="row row-cols-1 row-cols-md-2">
      {% for project in sorted_visuals limit: 2 %}
        {% include projects_horizontal.liquid %}
      {% endfor %}
      </div>
    </div>

    </div>
</div>
